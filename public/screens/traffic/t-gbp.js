'use strict';
S.TrafficGBP = {
  render(container, actions) {
    actions.innerHTML = '';
    container.innerHTML = '<div class="screen"><div class="card"><div class="empty"><div class="empty-title">Google Business Profile</div><div class="empty-sub">This screen is being built. Check back shortly.</div></div></div></div>';
  }
};
