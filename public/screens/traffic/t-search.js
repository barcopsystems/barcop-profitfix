'use strict';
S.TrafficSearch = {
  render(container, actions) {
    actions.innerHTML = '';
    container.innerHTML = '<div class="screen"><div class="card"><div class="empty"><div class="empty-title">Search and SEO</div><div class="empty-sub">This screen is being built. Check back shortly.</div></div></div></div>';
  }
};
