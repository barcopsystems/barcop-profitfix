'use strict';
S.TrafficReviews = {
  render(container, actions) {
    actions.innerHTML = '';
    container.innerHTML = '<div class="screen"><div class="card"><div class="empty"><div class="empty-title">Review Tracker</div><div class="empty-sub">This screen is being built. Check back shortly.</div></div></div></div>';
  }
};
