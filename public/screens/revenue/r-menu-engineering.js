'use strict';
S.RevenueEvents = {
  render(container, actions) {
    container.innerHTML = '<div class="screen"><div class="card"><div class="empty"><div class="empty-title">Events and Catering</div><div class="empty-sub">This screen is being built. Check back soon.</div></div></div></div>';
  }
};
